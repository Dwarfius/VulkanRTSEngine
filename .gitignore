# Ignore all the build directories
bin/
build/
x64/
# SpirV shader bytecode - just to avoid platform-dependent issues, force users to compile it on their own
*.spv

# RenderDoc files used for saving testing configurations
*.cap

# VSCode - need to ignore everything except 
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json